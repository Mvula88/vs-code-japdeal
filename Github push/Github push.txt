

Initialize Git here:

git init
git remote add origin https://github.com/Mvula88/vs-code-japdeal.git

Stage, commit, and push:

git add .
git commit -m "Initial commit - clean structure"
git branch -M main
git push -u origin main --force
